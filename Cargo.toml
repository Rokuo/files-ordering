[package]
name = "files-ordering"
version = "0.1.0"
description = "A file ordering app"
authors = ["Aleks Dems"]
edition = "2021"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tempfile = "3.5.0"

